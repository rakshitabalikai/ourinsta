import Nav from "./Nav";

function Reels(){
    return(
        <div>
            <Nav />
            <h1>Reels can be uplod here , we can watch the reels from here</h1>
        </div>

    );
}
export default Reels;